abstract final class SizeTokens {
  /// Button min height (touch target for filled/outlined buttons).
  static const double buttonMinHeight = 56;

  /// Icon Button min height and width (touch target).
  static const double iconButtonMinHeightWidth = 48;

  /// Icon size small (e.g. icon buttons, list item icons).
  static const double iconSizeSmall = 24;

  /// Avatar size small (icon/initial avatars).
  static const double avatarSmall = 40;

  /// Avatar size medium (e.g. image avatar).
  static const double avatarMedium = 56;
}
